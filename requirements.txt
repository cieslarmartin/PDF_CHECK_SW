# PDF_CHECK_SW – dependencies for web_app + desktop_agent
# Install: pip install -r requirements.txt

# --- web_app (Flask backend, Admin, API, Excel export) ---
Flask>=2.0.0
openpyxl>=3.0.0

# --- desktop_agent (API client, GUI, config) ---
requests>=2.28.0
PyYAML>=6.0
tkinterdnd2>=0.3.0
